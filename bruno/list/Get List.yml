info:
  name: Get List
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/list"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
